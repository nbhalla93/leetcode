class Solution:
    def reverseWords(self, s: str) -> str:
        list = s.split()
        result = ""
        for i in reversed(range(len(list))):
            result += str(list[i]) + " "
        return result.rstrip()
        
